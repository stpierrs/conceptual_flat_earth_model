# Change log

Detailed change history is maintained in the git commit log (`git log --oneline`).

The model runs on Ptolemy's epicycle ephemeris with a single Earth-focus
GeoC pipeline as the comparison alternative. AstroPixels (DE405 daily lookup
table) is reserved for eclipse-demo refinement only.
