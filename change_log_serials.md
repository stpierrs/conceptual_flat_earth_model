# Change log

Detailed change history is maintained in the git commit log (`git log --oneline`).

The browser app runs as a static JS site. The Rust core in `src/` provides a
native library and `fe-model` CLI for the ported computational model. See
`DEVELOPMENT.md` for run and usage commands.
